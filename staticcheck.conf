checks = ["all"]
initialisms = ["ID", "UUID", "API"]

[ignore]
# Allow time.Now only in the timeutil package itself
SA1019 = ["internal/utils/timeutil/timeutil.go"]

[custom."time.Now() must not be used directly; use timeutil.Now() instead"]
pattern = "time.Now()"
message = "Use timeutil.Now() instead of time.Now() to enforce UTC consistency"
