global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "my_application"
    static_configs:
      - targets: ["host.docker.internal:8000"] # Replace with your service

rule_files:
  - "../monitoring/prometheus-alerts.yml"  # Reference your alert rules
